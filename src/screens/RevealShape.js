import React from 'react'
import { Text, View } from 'react-native'

import AppLayout from '../layout/AppLayout'

const RevealShape = () => (
  <AppLayout>
    <View style={{ alignSelf: 'auto' }}>
      <Text>RevealShape</Text>
    </View>
  </AppLayout>
)

export default RevealShape
